路由嵌套规则


路由配置，在配置路由时，一级路由也称之为根路由。

它对象的是根组件里的路由视图组件，也就是说路由对象里所有的组件最终都会渲染到这个路由视图组件当中。



通过children属性，在任意一条路由对象中创建一个子路由表。

在遍历每一条对象头像时会检测是否有children这个属性并且是不是一个数组，如果有接着遍历。

每个子路由表里的组件都会映射到该父路由组件的路由视图组件上。