堆栈都是属于寄存器的一种逻辑结构，而这个逻辑结构都属于数据结构的一部分。

除了逻辑结构还有物理层结构，本章我们主要说逻辑结构。

堆栈是寄存器的一种数据结构，栈的结构是一种先进后出的结构，或者说是一种线性结构的结构图表，

排列规则是自下而上的顺序，这也正是它先进后出的规则。

线性结构中的数据之间是一对一的关系。

当向栈中写入数据时，我们称之为压栈或者进栈。

堆亦是寄存器的一种数据结构。

栈区是由编译器或者说操作系统自动分配释放的。

而堆区是由程序员自动分配和释放的，如果程序员不释放，当程序结束时会由操作系统回收。

一个堆区是由程序员通过new出来的内存。

