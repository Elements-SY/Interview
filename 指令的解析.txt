插值表达式的实现是根据挂载的DOM对象，通过遍历查询该元素对象的childNodes也就是所有的子节点的文本节点，元素节点和属性节点；

由于childNodes是一个伪数组，它的原型并不是指向数组原型，所有自然也就没有数组的方法，除非使用for循环来遍历childNodes；

但是这样太麻烦，我们可以通过[].slice.call(childNodes);把伪数组的this指向数组；这样伪数组就有了数组的方法；

当伪数组就有了数组的方法之后，我们就可以使用数组的方法遍历childNodes了，判断当前的是属于文本节点，元素节点和属性节点，

并且对其做处理。